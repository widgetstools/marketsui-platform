/** @type {import('tailwindcss').Config} */
module.exports = {
  // Enable dark mode by adding/removing the "dark" class on <html>
  darkMode: 'class',

  // Tell Tailwind which files to scan for class names.
  // Unused classes are removed from the production build automatically.
  content: [
    './src/**/*.{ts,tsx,html}',
    // Include the dock-editor package so its Tailwind classes are included
    '../../packages/react-tools/dock-editor/src/**/*.{ts,tsx}',
  ],

  theme: {
    extend: {
      // Map Tailwind utility classes to our CSS custom properties.
      // e.g. `bg-background` → hsl(var(--background))
      colors: {
        background:  'hsl(var(--background))',
        foreground:  'hsl(var(--foreground))',
        card: {
          DEFAULT:    'hsl(var(--card))',
          foreground: 'hsl(var(--card-foreground))',
        },
        popover: {
          DEFAULT:    'hsl(var(--popover))',
          foreground: 'hsl(var(--popover-foreground))',
        },
        primary: {
          DEFAULT:    'hsl(var(--primary))',
          foreground: 'hsl(var(--primary-foreground))',
        },
        secondary: {
          DEFAULT:    'hsl(var(--secondary))',
          foreground: 'hsl(var(--secondary-foreground))',
        },
        muted: {
          DEFAULT:    'hsl(var(--muted))',
          foreground: 'hsl(var(--muted-foreground))',
        },
        accent: {
          DEFAULT:    'hsl(var(--accent))',
          foreground: 'hsl(var(--accent-foreground))',
        },
        destructive: {
          DEFAULT:    'hsl(var(--destructive))',
          foreground: 'hsl(var(--destructive-foreground))',
        },
        border: 'hsl(var(--border))',
        input:  'hsl(var(--input))',
        ring:   'hsl(var(--ring))',
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
    },
  },

  plugins: [],
};
